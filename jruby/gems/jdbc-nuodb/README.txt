= jdbc-nuodb

* http://nuodb.com/

== DESCRIPTION:

This gem bundles the NuoDB JDBC driver for JRuby. 

Use

   require 'jdbc/nuodb'

to make the driver accessible to JDBC and ActiveRecord code running in JRuby.

